import React from "react";
import * as d3 from "d3";
import { svg } from "d3";

const Areachart = ({ dataElement }) => {
  console.log(dataElement);

  return (
   
    <div>{dataElement.date}</div>
  )
    
};

export default Areachart;
